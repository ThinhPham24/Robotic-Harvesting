# harvest_digital_twin

Synchronizes real and simulated telemetry and publishes model residuals.

The starter `twin_monitor` compares named joints from `/joint_states` and
`/harvest/twin/joint_states`. Future work adds Gazebo launch, sensor twins,
payload/delay models, fault injection, and parameter identification.
